// import { userReducer } from "react"

const initialState = {
    email: null,
    firstName: null,
    lateName: null
}

export default function userReducer(state = initialState, action) {
    return state;
}